# Dockerfile.nginx
FROM nginx:stable-alpine

# Copy our vhost config into the image
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Make sure the web root exists (PHP app code is mounted at runtime)
RUN mkdir -p /var/www/html

WORKDIR /var/www/html
